# 2026-08-10 工作日志

## 重写字段表整理文档_完整版.md（PRD 化）
- 用户要求：以 HTML 实际渲染为准，补充弹窗/详情页与不同状态下展示差异，但**只用展示名称（不要代码级字段名）**；剔除通讯/IM 内容；**不要更新日志、不要删除记录等元信息**；不改 HTML。
- 重写 `Summary/字段表整理文档_完整版.md`（由 979 行精简至 818 行）：
  - 删除原「第八章 修订记录」整章、IM 沟通监控章节、所有「本次补录/删除/此前缺失」等元标注、以及所有 `mall.js:xxx`/`status===`/`MockData.` 等代码引用。
  - 五大端口全面补充弹窗/详情页与状态分支：运营端（订单履约详情页、审核合同/付款、客户详情、审核商家、竞价资源/公告审核与出价明细、配置抽佣、发布协议、文件预览等）；商家 PC（订单详情、发货、合同签署、开票、定标、资源/公告/上架弹窗）；商家 H5；买家 PC（商品/竞价详情状态分支、订单详情状态分支、合同/付款/发票弹窗、我参与的竞价四段式按钮）；买家 H5（各 Sheet 状态分支）。
  - 保留全平台状态枚举、排序规则、触发机制汇总（已去代码化表述）。
- 文档定位明确为"产品经理 PRD 素材"，仅描述界面展示内容与状态差异；与代码冲突仅在个别处用一句话产品备注（如买家可见查看报价、运营端已结束误显竞价中、协议管理无菜单入口）。

## 追加：彩色状态流转流程图（基于备份参考样式）
- 用户参考旧版 `字段表整理文档.md` 的 Mermaid 流程图配色约定，要求把"不同状态下不同角色有不同按钮操作"用颜色区分并入 完整版.md。
- 在 完整版.md 顶部新增「流程图颜色图例」：🟦蓝=流程状态 / 🟩绿=操作按钮 / 🟨黄=审核判断节点 / ⬜灰=结束状态。
- 全文统一为 4 色 classDef（state/btn/judge/endState）。原 2 张图（订单履约、竞价流程）上色并补全角色按钮节点；新增 5 张：商家入驻生命周期、上架商品审核下架、供需求购撮合、发票开具流转、竞价资源审核。
- 两张多角色操作权限矩阵上方加注"按钮=绿色操作按钮，同状态三端按钮不同"。
- 仍遵守：只用展示名称、不含 IM、不含更新日志/删除记录、不改 HTML。文档由 818→955 行。

## 修正：第七章触发机制中的"伪自动流转"误写 + 补后端自动化规范
- 用户指出"看货报名倒计时封签"等内容他没在 HTML 见过 → 核查 JS 确认：原型**无任何前端定时器**，所有状态流转均为点击/审核/事件驱动。此前我写的"时间驱动型机制"三行属于臆造（含不存在的"封签"概念）。
- 已修正/删除的误写：
  - 删除「看货报名倒计时封签→自动关闭看货报名」整行（"封签"在代码中不存在）。
  - 「竞拍截止自动进入等待公布」改为"后端定时任务扫描 bidEndTime 归零推进"（明确为待后端实现，非前端已实现）。
  - 「商品库存归零自动售罄」标注为后端库存钩子，原型仅种子数据。
  - 上架商品图（161 行）与竞价流程图（602 行）的"自动归零/到达截止"边改为"后端定时任务扫描"。
  - 商家端 341 行"系统自动转等待公布"改为后端定时任务推进。
- 保留的真实自动项：发布新版协议自动注销旧版（admin.js:2178 确认）；事件驱动的定标/撮合自动生成订单（merchant.js:1519 确认）。
- 新增「后端自动化流转落地说明」：定时任务扫描截止时间字段、幂等+乐观锁、竞价公告双状态表达（数值状态 vs 标题前缀）需统一、订单待付款超时自动取消建议。
- 经验：本类 PRD 文档描述"自动流转"前必须先 grep JS 确认是否存在定时/调度逻辑，不可凭业务想象补全。

## 二次修正：竞价时间字段过度描述 + 第七章改为 Mermaid 自动化流转图
- 用户指出 HTML 里竞价公告**只有「起拍底价」和「竞拍截止时间」两个时间字段**，没有"现场看货截止时间/看货报名截止时间"这类截止时间。核查 HTML 确认：merchant.html/h5 仅起拍底价+竞拍截止时间；admin.html:1162 的"现场看货截止时间"位于无触发入口的死弹窗（modal-admin-edit-ann）；merchant-h5.html:326 仍真实存在"看货报名截止时间"（代码层面，与产品口径冲突，已提醒用户）。
- 已删除文档中所有"现场看货截止时间/看货报名截止时间"提及，含：公告编辑表单（352 行）、H5 公告表单（646 行）、第七章"看货报名入口关闭"伪自动行（929 行）及落地说明中的 viewEndTime 引用。竞价公告表单现仅余 起拍底价 + 竞拍截止时间。
- 第七章整章重写为 **Mermaid 全系统自动化流转图**（⚡ 边=系统自动触发：竞拍截止→等待公布、库存归零→已售罄、发布新版协议→注销旧版、定标/撮合→自动生成订单、待付款超时→已取消），并保留流程图 4 色 classDef。删除原"后端定时任务扫描/待后端实现/钩子/种子数据/落地说明"等"让后端去完成"式措辞与整段"落地说明"。
- 用户明确：自动化不写成"标题里让后端去完成"，而是用 mermaid 图形表达；且不要"落地说明"类内容。
- 校验：grep 全文无 封签/现场看货截止/看货报名截止/落地说明/后端定时任务/bidEndTime/viewEndTime/待后端实现 等残留。文档现 966 行。

## 第七章：补全自动化流转的"文字说明"
- 用户要求：第七章全自动流转不仅要 mermaid 图，**还要文字说明**（但仍不要"落地说明"/后端实现指引）。同时"merchanth5 不用管"——H5 商家端相关不匹配项（MerchantH5App 崩溃、死代码弹窗等）移出文档范围。
- 在 第七章 mermaid 图后新增「自动化流转文字说明」：逐条描述 5 类 ⚡ 系统自动触发（竞价截止→等待公布、待付款超时→已取消、库存归零→已售罄、发布新版协议→注销旧版、定标/撮合→自动生成交易订单），并附汇总表。表述为产品行为规格（触发条件/源→目标状态/系统动作），不含后端架构/实现指引。
- 文档现 993 行。

## 补充：H5 移动买家端（h5.html/h5.js）整页结构
- 用户要求：以 HTML 实际渲染为准，把 H5 买家端**完整页面结构**补进文档（此前仅写了 Sheet/弹窗，缺页面级结构）。"merchanth5 不用管"仍适用于 H5 商家端；聊天(IM)按既定口径不收录。
- 重写 第五章（五、H5 移动买家端），由单表扩充为 0️⃣~1️⃣2️⃣ 小节：整体导航(4 Tab)/首页/全部分类页/找货源/竞价大厅/购物车(按店分组+失效+结算栏)/个人中心/商铺主页+搜索商户/我的订单(列表+详情)/我的竞价记录/发票中心/实名企业认证/弹窗与详情Sheet。
- 修正一处过度描述：订单详情原写"物流时间轴(6节点)"，但 h5.html 仅有「物流发运信息(快递单号)」、无时间轴 DOM，已改为如实描述。

## 细化：H5 整节升级为标准字段表格式
- 用户要求：H5 整节（五、H5 移动买家端）补成「字段内容表格 + 相关表单 + 表单排序 + 流程状态」的标准格式（与其他详尽小节一致）。仅 H5 买家端，merchanth5(商家H5)仍不碰。
- 将第五章每个小节（0️⃣~1️⃣2️⃣）由要点罗列改写为：列表字段链 + 四列字段内容表(字段名称/类型/展示与格式要求/触发动作) + 弹窗表单字段 + 默认排序行 + 流程状态枚举(订单/竞价/发票/认证)。保留并核实 h5.html 展示名称（如 立即采购、确认签名并盖章、纳税人识别号只读）。
- 文档由 1069→1225 行。其余端口未改动；七章 mermaid 被 Obsidian 渲染器改写成 flowchart TB 格式（仍有效，无 endStateState 残留）。
- 仅用展示名称；无代码字段名/行号/IM/更新日志。文档现 1069 行。

## 修正：货品卡片旁"数量步进器 = 该货品最低起售数量"
- 用户澄清产品规则：**商品卡（列表/网格）旁的"数量步进器"不是固定默认 1，而是该货品的最低起售数量**，作为加入购物车时的采购数量。
- 核查 HTML 确认卡片旁步进器真实存在：Mall `mall.js` 在 首页精选/全部分类/现货大厅/搜索结果 的商品卡悬浮层均有 `qty-*`（−/数字/＋）+「加入采购」；H5 `h5.js:279-290` 首页/商铺商品卡有 `qty-in`（−/数字/＋）+ 圆形购物车按钮。详情弹窗/详情 Sheet 内另有独立的"意向采购数量/采购数量"步进器。
- 文档改动（Summary/字段表整理文档_完整版.md，mall 与 h5 两端口）：
  - 新增"商品卡旁数量步进器（默认填入该货品最低起售数量）"描述：Mall 2️⃣ 现货大厅卡片字段 + 复用说明（首页/分类/大厅/搜索/商铺同款）；Mall 1️⃣ 首页精选推荐补一句；H5 1️⃣ 首页商品网格字段链与字段表新增"数量步进器"行。
  - 修正原误写"默认 1 / 最小1"：Mall 2️⃣ 商品详情"意向采购数量"步进器、H5 1️⃣2️⃣ 汇总"采购数量步进器"均改为"默认填入该货品的最低起售数量"。
  - 保留 H5 5️⃣ 购物车"数量步进器（最小 1）"不动——它是购物车内的数量调整控件（不在货品卡片旁），下限 1 为购物车级约束，与"卡片旁=最低起售数量"是两回事。
- 约束仍守：只用展示名称、不改 HTML、与 HTML 真实渲染一致。
- 经验：商品卡（卡片旁）步进器 与 详情弹窗内采购数量步进器 是两套独立控件，写文档时勿混为一谈。

## 续：审计并修正「订单详情」详情页（与 HTML 对齐）
- 用户要求继续核查"HTML 有而文档没写/写错"的详情页。重做一遍审计：列出全部 `view-*`/`modal-*`/`sheet-*` ID + grep 所有 JS 动态 `createElement('div')` 弹窗，逐一比对文档。
- 发现核心不一致：**Mall `5️⃣ 个人中心与订单履约` 与 Merchant `4️⃣ 商家订单中心` 的"订单详情页"描述的是错误步骤名（Mall: 提交订单→双方签约→托管付款→商家发货→确认收货→交易完结；Merchant: 提交买单→合同签署及审核→资金托管→卖家发货→确认收货→结算出账）且错称"详情页内有操作按钮"**。实际两者共用同一 JS 弹窗 `components.js showOrderDetail`（`modal-order-detail`）：860px 居中卡片、深色顶栏、**只读（仅"关闭窗口"按钮，所有操作在订单列表卡片上）**；内容为：6 步履约进度条（订单创建→合同签署→合同审核→打款与核销→物流发货→签收结清，已完成绿✓/当前紫/未达灰）、供应商家、卖家主体公司、财务金额与平台抽佣计算卡（订单成交总额红/平台抽佣费率蓝/预计平台抽佣金额紫/资金流转路径）、订购商品明细表（序号/货品名称/单价(依据合同约定标价)/履约数量(大宗按批交割)/小计金额红）、买方与卖方合同签署状态（已审核通过绿/已打回红/已上传待审核黄/待上传合同灰）。
- 已用标准四列字段表（字段名称/类型/展示与格式要求/触发动作）重写 Mall 5️⃣ 与 Merchant 4️⃣ 的订单详情，并注明两端为同一通用弹窗、操作在列表。
- 已验证其余端口订单详情**本就正确，未改**：H5 `8️⃣` 用独立静态页 `view-uc-order-detail`（状态横幅/货品卡含平台佣金1.5%/物流/合同凭证，文档 line 1142 已对应）；Admin `5️⃣` 用独立静态区 `admin-order-detail-section`，其 JS（`admin.js:2549-2554`）步骤恰为"提交买单→合同签署及审核→资金托管→卖家发货→确认收货→结算出账"，与文档一致；买家"申请发票"弹窗 `components.js showInvoiceModal` 与文档 Mall `申请发票` 行对齐。
- 文档现 1339 行；mermaid 8/16 围栏平衡；无 endStateState 残留。
- 经验：审计"订单详情"类详情页时，必须分清三类实现——(a) 多端共用同一 JS 弹窗（Mall/Merchant 的 showOrderDetail），(b) 某端独立静态页（H5 view-uc-order-detail），(c) 某端独立静态区（Admin admin-order-detail-section）；步骤条措辞因端口而异，须逐端比对 JS，不可套用一端描述。

## 修正：订单履约"资金托管"概念 → 买家的付款 + 付款后的审核
- 用户明确：**产品里没有"资金托管/托管账户/托管付款"这套资金存管概念**；付款阶段就是"一个买家的付款"和"一个付款后的审核"（即买家付款 + 平台对账审核凭证）。要求把文档里所有"资金托管"都改掉。
- 文档改动（Summary/字段表整理文档_完整版.md）：
  - Admin `5️⃣` 履约节点进度由 6 步改 **7 步**：`1.提交买单→2.合同签署及审核→3.买家的付款→4.付款后的审核→5.卖家发货→6.确认收货→7.结算出账`；左栏"支付凭证与资金托管存证"→"支付凭证与付款审核存证"；状态表进度条位置（待发货第5步/待签收第6步/已完成第7步全勾）；流转 mermaid 的 `核销通过`→`付款审核通过`。
  - Mall `5️⃣` 与 Merchant `4️⃣` 共用弹窗的"打款与核销"（实为同一概念的不同措辞）统一替换为 `买家的付款 → 付款后的审核`，进度条也由 6 步改 7 步，三端术语一致。
  - 文档已无"资金托管""打款与核销"残留；mermaid 8/16 围栏平衡；文档现 1339 行。
- **未改 HTML**：原型 `platform/` 在 admin.js/merchant.js/mall.js/h5.js/components.js/mall.html 共 40+ 处仍用"资金托管/托管账户/托管付款/打款与核销"（含 components.js:107/109 合同模板正文、mall.js:1564 自有 steps 数组、各端支付凭证说明）。按用户既定"不改 HTML"约束，本次只改文档；已向用户说明并询问是否同步改原型代码。
- 经验：当用户纠正业务语义（如"没有资金托管"）时，文档应以用户口径为准统一术语，即便与 HTML 原型当前措辞不同；HTML 的同步需另行确认（涉及 step 数组下标重排、合同模板正文等多处，有破坏原型风险）。

## 续：同步改原型代码 `platform/` 的"资金托管"措辞（用户确认要改）
- 用户第二条指令："把代码里面的资金托管都改了吧，不要动其他地方的内容"。已对 `platform/` 全量 grep 定位 40+ 处并逐一改完。
- 改动文件与要点：
  - `admin.js` / `merchant.js`：订单详情 steps 数组 `3.资金托管`→`3.买家的付款`+`4.付款后的审核`（顺延卖家发货→5/确认收货→6/结算出账→7）；`currentStep` 映射 status1/2/3 由 3/4/5 改为 4/5/6；进度条分母 `(currentStep/5)`→`(currentStep/6)`；banner/审核通过 toast/支付凭证标题/凭单名 同步替换；`平台监管账户`→`平台对公收款账户`。
  - `mall.js`：买家 steps 数组 `托管付款`→`买家的付款`+`付款后的审核`（7 元素）；statusMap 的 `step` 索引 1/2/3/4→2/4/5/6；`平台监管托管账户`→`平台对公收款账户`；`线上担保支付 (托管账户)`→`线上担保支付`。
  - `components.js`：共享弹窗 `showOrderDetail` 的 stepItems `4.打款与核销`→`4.买家的付款`+`5.付款后的审核`（物流发货→6/签收结清→7）；`isCurrent` 高亮索引同步；合同模板正文"入托管账户"→"并完成付款"、"平台监管托管"→"统一结算"；`托管状态: 平台监管中`→`付款状态: 待审核`。
  - `h5.js`：时间线条目 `货款资金平台托管入账`→`买家的付款`+`付款后的审核`；`currentStep` 映射 1/2/3/4/5→2/4/5/6；`平台监管账户`→`平台对公收款账户`；凭单名 `平台托管资金入账单/《平台托管账户划转确认函》`→`平台付款审核入账单/《平台付款审核划转确认函》`。
  - `mall.html`：`线上担保支付 (托管账户)`→`线上担保支付`；商品说明 `园区安全沙盒核销监管`→`园区安全沙盒审核监管`；三端 HTML 与 JS 里 `6步流程进度条/6步可视化流程` 注释改为 `7步`。
- 刻意保留未动的：所有"打款凭证/打款金额"指买家付款回单/凭证（非托管概念）；竞价弹窗 `包含6步流程` 注释（属竞价自身流程，非订单履约）。
- 校验：grep `资金托管|托管账户|托管付款|托管支付|托管资金|托管状态|打款与核销|核销监管|监管账户|监管托管|入托管账户` 全平台 0 命中。
- 经验：改步骤条文案若把一步拆两步，必须同步改 `currentStep` 映射与进度条分母，否则进度条错位；用 substring 替换比整行替换更稳（避免缩进/不可见字符导致整行匹配失败）。
